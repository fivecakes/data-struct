#include "pfs.h"


void pfs(struct graph *g,int s,void prio_updater(void))
{
    //reset();
//    int v = s;
//    do {
//        if (g->graph_nodes[v].status == UNDISCOVERED) {
//            pfs_(g,v,prio_updater);
//        }
//    } while (s !=(v=(++v)));
}
